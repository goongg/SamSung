#include <iostream>
#include <vector>

using namespace std;

int t;
int m, c, i;
int main()
{  
    cin>>t;
    while(t-- >0)
    {
        cin>>m >>c>>i;

        vector<char> array(m);
        vector<char> code(c);
        vector<char> data(i);
        vector<int> go(m);

        for(int i =0 ; i<c; i++)
        {
            
        }

        while(1)
        {
            int p = 0;
            char oper = code[p];
            if(oper =='-')
            {}
            else if(oper == '+')
            {}
            else if(oper == '<')
            {}
            else if(oper == '>')
            {}
            else if(oper == '[')
            {}
            else if(oper == ']')
            {}
            
        }


    }    


   return 0;
}